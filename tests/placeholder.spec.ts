import { test, expect } from "@playwright/test";

test("placeholder e2e passes", async () => {
  // Lightweight sanity check to keep the e2e suite green until real tests are added.
  expect(true).toBe(true);
});
